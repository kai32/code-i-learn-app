<% set_title "#{@category.title} | Featured articles" %>
<% set_description "Featured articles written by users on #{@category.title}" %>

<h1>Featured articles for <%= @category.title %></h1>

<%= render 'articles/articles', obj: @articles %>

<div class="row">
  <div class="pull-right">
    <%= will_paginate @articles %>
  </div>
</div>