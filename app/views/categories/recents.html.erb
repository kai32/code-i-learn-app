<% set_title "#{@category.title} | Recent articles" %>
<% set_description "Recent articles written by users on #{@category.title}" %>
<h1>Recent articles for <%= @category.title %></h1>

<%= render 'articles/articles', obj: @articles %>

<div class="row">
  <div class="pull-right">
    <%= will_paginate @articles %>
  </div>
</div>